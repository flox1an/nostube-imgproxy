# Build configuration for AVIF support
# The AVIF decoder requires meson and ninja to build dav1d internally
# Make sure these are installed: pip3 install --user meson ninja

[env]
SYSTEM_DEPS_DAV1D_BUILD_INTERNAL = "always"
